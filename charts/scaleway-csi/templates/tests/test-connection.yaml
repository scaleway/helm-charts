apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scaleway-csi.fullname" . }}-test-connection"
  labels:
    {{- include "scaleway-csi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scaleway-csi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
